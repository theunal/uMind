  name: umind
  description: "uMind - Görsel IQ Testi"
  publish_to: 'none'
  version: 1.0.0+1

  environment:
    sdk: ^3.12.2

  dependencies:
    flutter:
      sdk: flutter
    flutter_localizations:
      sdk: flutter
    cupertino_icons: ^1.0.8
    hive: ^2.2.3
    hive_flutter: ^1.1.0
    path_provider: ^2.1.2
    provider: ^6.1.2
    google_fonts: ^8.1.0
    go_router: ^17.3.0

  dev_dependencies:
    flutter_test:
      sdk: flutter
    flutter_lints: ^6.0.0
    hive_generator: ^2.0.1
    build_runner: ^2.4.13

  flutter:
    uses-material-design: true
    assets:
      - assets/questions/
